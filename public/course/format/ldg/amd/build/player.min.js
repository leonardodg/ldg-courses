/**
 * Ajusta a altura do quadro da aula.
 *
 * O quadro carrega uma pagina do PROPRIO site, entao ele e mesma origem e da
 * para ler a altura do documento direto. Isso evita o vaivem de postMessage que
 * este tipo de embutido costuma ter - e evita tambem ter que instalar um
 * ouvinte em toda pagina do Moodle so para responder "que altura voce tem".
 *
 * O ResizeObserver cobre o que o evento load nao cobre: quiz que abre uma
 * pergunta, imagem que termina de carregar, secao que expande. Sem ele a altura
 * fica travada no que era no primeiro instante, e o conteudo ganha uma barra de
 * rolagem interna - a rolagem dentro da rolagem que ninguem consegue usar.
 *
 * ESTE ARQUIVO NAO PASSA POR TRANSPILADOR. O requirejs.php serve o amd/src
 * quando nao existe .map, entao o codigo aqui precisa ser AMD de verdade.
 *
 * @module     format_ldg/player
 * @author     LeoDG <callme@leodg.dev>
 * @copyright  2026 LeoDG <callme@leodg.dev>
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("format_ldg/player",["core/log"],(function(Log){var resize=function(frame){var doc;try{doc=frame.contentDocument}catch(e){return void Log.debug("format_ldg/player: quadro de outra origem, mantendo a altura minima.")}if(doc&&doc.body){var current=parseInt(frame.style.height,10)||0;frame.style.height="0px";var content=Math.max(doc.documentElement?doc.documentElement.scrollHeight:0,doc.body.scrollHeight),newheight=Math.max(content+24,420);frame.style.height=newheight+"px",newheight!==current&&Log.debug("format_ldg/player: quadro ajustado para "+newheight+"px.")}},observe=function(frame){if(resize(frame),"undefined"!=typeof ResizeObserver){var doc=null;try{doc=frame.contentDocument}catch(e){return}if(doc&&doc.body)new ResizeObserver((function(){resize(frame)})).observe(doc.body)}};return{init:function(cmid){var frame=document.getElementById("ldg-lesson-frame-"+cmid);frame&&(frame.style.height="420px",frame.addEventListener("load",(function(){observe(frame)})),frame.contentDocument&&"complete"===frame.contentDocument.readyState&&observe(frame))}}}));

//# sourceMappingURL=player.min.js.map