/**
 * Barra de secoes da landing de parceiros.
 *
 * Faz duas coisas, e as duas por medicao e nao por valor fixo:
 *
 * 1. Publica a altura do cabecalho FIXO do tema em --ldgp-sticky-top. A altura
 *    muda por tema, e no theme_ldg muda tambem entre celular e desktop.
 *    Hardcodar 64px quebra no Moove e quebra no celular do ldg.
 *
 * 2. Marca a secao em vista com aria-current. Por IntersectionObserver, e nao
 *    por evento de rolagem: o observer nao roda a cada pixel.
 *
 * O sticky em si e CSS. Este modulo so alimenta o numero de que o CSS precisa -
 * se ele nao carregar, a barra continua grudando, so que no topo do viewport.
 *
 * NAO ha transpilador neste projeto: este arquivo precisa ser AMD de verdade.
 *
 * @module     local_partners/sectionbar
 * @copyright  2026 LeoDG <callme@leodg.dev>
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_partners/sectionbar",[],(function(){var measureHeader=function(){var bar=document.querySelector(".ldgp-bar");if(bar){var header=document.querySelector(".navbar.fixed-top"),height=header?Math.round(header.getBoundingClientRect().height):0;bar.style.setProperty("--ldgp-sticky-top",height+"px");var page=document.querySelector(".ldgp");page&&page.style.setProperty("--ldgp-sticky-top",height+"px")}},observeSections=function(){var links=Array.prototype.slice.call(document.querySelectorAll('.ldgp-bar-link[href^="#"]'));if(links.length&&window.IntersectionObserver){var byId={},sections=[];links.forEach((function(link){var id=link.getAttribute("href").slice(1),section=document.getElementById(id);section&&(byId[id]=link,sections.push(section))}));var observer=new window.IntersectionObserver((function(entries){entries.forEach((function(entry){var id;entry.isIntersecting&&(id=entry.target.id,links.forEach((function(link){link.removeAttribute("aria-current")})),byId[id]&&byId[id].setAttribute("aria-current","true"))}))}),{rootMargin:"-20% 0px -70% 0px",threshold:0});sections.forEach((function(section){observer.observe(section)}))}};return{init:function(){var fn,wait,timer;measureHeader(),observeSections(),window.addEventListener("resize",(fn=measureHeader,wait=150,timer=null,function(){window.clearTimeout(timer),timer=window.setTimeout(fn,wait)}))}}}));

//# sourceMappingURL=sectionbar.min.js.map